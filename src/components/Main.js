import SearchLocation from "./SearchLocation";
function Main({ changeLocation }) {
  return (
    <div>
      <SearchLocation changeLocation={changeLocation} />
    </div>
  );
}

export default Main;
